```ts
// Button.stories.ts

import Button from './button.component';

export default {
  title: 'Button',
  component: Button,
  parameters: {
    myAddon: {
      data: 'this data is passed to the addon',
    },
  },
};
```
