```ts
// Button.stories.ts

import Button from './button.component';

export const Primary = () => ({
  component: Button,
  props: {
    label: 'Button',
    background: '#ff0',
  },
});

export const Secondary = () => ({
  component: Button,
  props: {
    label: '😄👍😍💯',
    background: '#ff0',
  },
});

export const Tertiary = () => ({
  component: Button,
  props: {
    label: '📚📕📈🤓',
    background: '#ff0',
  },
});
```
