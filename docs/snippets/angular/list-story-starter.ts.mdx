```ts
// List.stories.ts

import List from './list.component';

export default {
  title: 'List',
  component: List,  
};

// Always an empty list, not super interesting
const Template = (args: List) => ({
  component: List,
  props: args,
});

```