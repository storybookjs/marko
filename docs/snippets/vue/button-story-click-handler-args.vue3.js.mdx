```js
// Button.stories.js

export const Text = (args) => ({
  components: { Button },
  setup() {
    return {
      label: args.label,
      onClick: action('clicked'),
    };
  },
  template: '<Button @onClick="onClick" :label="label" />',
});
Text.args = {
  label: 'Hello',
};
```