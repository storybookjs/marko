```md
<!-- Table.stories.mdx -->

export const TableStory = ({ data, ...args }) => (
  <Table {...args} >
    {data.map(row => (<TR>{row.map(item => <TD>{item}</TD>}</TR>))}
  </Table>
);

<Story
  name="Numeric"
  args={{
    data: [[1, 2, 3], [4, 5, 6]],
    size: 'large',
  }}
>
  {TableStory.bind({})}
</Story>
```
