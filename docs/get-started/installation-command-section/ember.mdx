Use the Storybook CLI to install it in a single command. First, run the below commands inside your existing project’s root directory.

```shell
# Add Storybook:
npx -p @storybook/cli sb init

# Add Ember storybook adapter
ember install @storybook/ember-cli-storybook

```

If you run into issues with the installation, check the troubleshooting section below for guidance on how to solve it.