import { Story } from '@storybook/components';
import { action } from '@storybook/addon-actions';
import { VictoryPie, Slice } from 'victory';
import { Button } from '@storybook/react/demo';

export const componentMeta = {
  title: 'MDX|Victory',
  decorators: [story => <div style={{ width: 400, height: 400 }}>{story()}</div>],
};

# VictoryPie

It's never been easier to create beautiful pie charts, so let's do it.

## Default

It's filled in by default, so you can be really lazy and still get good results

<Story name="by default">
  <div style={{ width: 400, height: 400 }}>
    <VictoryPie />
  </div>
</Story>

## Basic data

Or you can give it your own data

<Story name="with basic data">
  <div style={{ width: 400, height: 400 }}>
    <VictoryPie
      data={[{ x: 'Cat', y: 62 }, { x: 'Dog', y: 91 }, { x: 'Fish', y: 55 }, { x: 'Bird', y: 55 }]}
    />
  </div>
</Story>

## Fancy

Let's get fancy!!!

<Story name="fancy">
  <div style={{ width: 400, height: 400 }}>
    <VictoryPie
      style={{
        labels: {
          fill: 'white',
          fontSize: 12,
          fontWeight: 'bold',
        },
      }}
      data={[
        { x: '<5', y: 6279 },
        { x: '5-13', y: 9182 },
        { x: '14-17', y: 5511 },
        { x: '18-24', y: 7164 },
        { x: '25-44', y: 6716 },
        { x: '45-64', y: 4263 },
        { x: '≥65', y: 7502 },
      ]}
      innerRadius={90}
      labelRadius={110}
      colorScale={['#D85F49', '#F66D3B', '#D92E1D', '#D73C4C', '#FFAF59', '#E28300', '#F6A57F']}
    />
  </div>
</Story>
