import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';

import Sidebar from './Sidebar';
import { standardData } from './SidebarHeading.stories';
import { mockDataset } from './treeview/treeview.mockdata';

<Meta component="Sidebar" title="Addons/Docs/Sidebar" />

# Sidebar

Here's some custom documentation for Storybook's `Sidebar` component.

<Preview>
  <Story name="simple" height="150px">
    <Sidebar
      menu={standardData.menu}
      stories={mockDataset.withRoot}
      storyId="1-12-121"
    />
  </Story>
</Preview>

<Preview>
  <Story name="isLoading" height="150px">
    <Sidebar menu={standardData.menu} stories={{}} isLoading />
  </Story>
</Preview>
